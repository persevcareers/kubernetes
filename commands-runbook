Section1 commands:
#creation of pod - imperative,
kubectl run nginx-app --image=nginx --port=80 

#creation of service to expose application running inside pod,  
kubectl expose pod nginx-app --type=NodePort --name=nginx-service --port=80

#creation of service to expose application runnig inside pod with nodeport value
kubectl expose pod nginx-app --type=NodePort --name=webapp-service1 --port=80 --target-port=80 --node-port=32000

#List the pods
kubectl get pods

#List the node in the cluster
kubectl get nodes

#List the sevices in the cluster
kubectl get services
**scaling replicas**
kubectl scale rs nginx-rs --replicas=5

#The NoSchedule pods on master
kubectl taint nodes ip-172-31-10-149  node-role.kubernetes.io/master:NoSchedule
#command to label nodes under ROLES as required
kubectl label nodes  ip-172-31-10-149 node-role.kubernetes.io/master=Master

namespace
Creating namespaces:
kubectl apply -f create-namespace.yml
Get ns:
kubectl get ns

scheduling-nodeselector
To label : 
kubectl label nodes node-name key=value
example
kubectl label nodes ip-172-31-34-137 node=medium

to unlabel:
kubectl label nodes ip-172-31-34-137 node-

rollout:
kubectl rollout history deployment <deployment-name>

kubectl rollout status deployment <deployment-name>

kubectl rollout pause deployment <deployment-name>


kubectl rollout resume deployment <deployment-name>


kubectl rollout restart deployment <deployment-name>


kubectl rollout undo deployment <deployment-name> 


kubectl rollout undo deployment <deployment-name> --to-revision=<revision>


create secrets:
kubectl create secret docker-registry dockerhub-creds --docker-server=docker.io --docker-username=persevcareers6577 --docker-password=Passw0rd03




